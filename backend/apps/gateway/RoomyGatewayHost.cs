namespace SmartSolutionsLab.Roomy.Gateway;

// Marker type so the WebApplicationFactory integration tests can target this host's assembly. A
// namespaced marker is used instead of the top-level-statement `Program` so the entry point the tests
// boot is named and unambiguous, mirroring the other context hosts.
public sealed class RoomyGatewayHost;
