{
  "name": "identity-api",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/identity/api/src",
  "prefix": "roomy",
  "projectType": "library",
  "tags": ["type:api", "context:identity"],
  "targets": {
    "generate-client": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ng-openapi-gen --config frontend/libs/identity/api/ng-openapi-gen.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/identity/api"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
