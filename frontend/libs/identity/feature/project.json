{
  "name": "identity-feature",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/identity/feature/src",
  "prefix": "roomy",
  "projectType": "library",
  "tags": ["type:feature", "context:identity"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/identity/feature"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
