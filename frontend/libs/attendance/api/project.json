{
  "name": "attendance-api",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/attendance/api/src",
  "prefix": "roomy",
  "projectType": "library",
  "tags": ["type:api", "context:attendance"],
  "targets": {
    "generate-client": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ng-openapi-gen --config frontend/libs/attendance/api/ng-openapi-gen.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/attendance/api"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
