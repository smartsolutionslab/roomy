/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfficeResponse } from '../../models/office-response';
import { RenameOfficeRequest } from '../../models/rename-office-request';

export interface RenameOffice$Params {
  officeId: string;
      body: RenameOfficeRequest
}

export function renameOffice(http: HttpClient, rootUrl: string, params: RenameOffice$Params, context?: HttpContext): Observable<StrictHttpResponse<OfficeResponse>> {
  const rb = new RequestBuilder(rootUrl, renameOffice.PATH, 'patch');
  if (params) {
    rb.path('officeId', params.officeId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfficeResponse>;
    })
  );
}

renameOffice.PATH = '/offices/{officeId}/name';
