/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HiredEmployeeResponse } from '../../models/hired-employee-response';
import { HireEmployeeRequest } from '../../models/hire-employee-request';

export interface HireEmployee$Params {
      body: HireEmployeeRequest
}

export function hireEmployee(http: HttpClient, rootUrl: string, params: HireEmployee$Params, context?: HttpContext): Observable<StrictHttpResponse<HiredEmployeeResponse>> {
  const rb = new RequestBuilder(rootUrl, hireEmployee.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HiredEmployeeResponse>;
    })
  );
}

hireEmployee.PATH = '/employees';
