{
  "name": "organization-api",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/organization/api/src",
  "prefix": "roomy",
  "projectType": "library",
  "tags": ["type:api", "context:organization"],
  "targets": {
    "generate-client": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ng-openapi-gen --config frontend/libs/organization/api/ng-openapi-gen.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/organization/api"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
