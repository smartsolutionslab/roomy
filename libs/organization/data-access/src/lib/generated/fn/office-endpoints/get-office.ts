/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfficeResponse } from '../../models/office-response';

export interface GetOffice$Params {
  officeId: string;
}

export function getOffice(http: HttpClient, rootUrl: string, params: GetOffice$Params, context?: HttpContext): Observable<StrictHttpResponse<OfficeResponse>> {
  const rb = new RequestBuilder(rootUrl, getOffice.PATH, 'get');
  if (params) {
    rb.path('officeId', params.officeId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfficeResponse>;
    })
  );
}

getOffice.PATH = '/offices/{officeId}';
