/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateOfficeRequest } from '../../models/create-office-request';
import { OfficeResponse } from '../../models/office-response';

export interface CreateOffice$Params {
      body: CreateOfficeRequest
}

export function createOffice(http: HttpClient, rootUrl: string, params: CreateOffice$Params, context?: HttpContext): Observable<StrictHttpResponse<OfficeResponse>> {
  const rb = new RequestBuilder(rootUrl, createOffice.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfficeResponse>;
    })
  );
}

createOffice.PATH = '/offices';
