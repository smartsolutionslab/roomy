/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfficeResponse } from '../../models/office-response';
import { RenameRoomRequest } from '../../models/rename-room-request';

export interface RenameRoom$Params {
  officeId: string;
  roomId: string;
      body: RenameRoomRequest
}

export function renameRoom(http: HttpClient, rootUrl: string, params: RenameRoom$Params, context?: HttpContext): Observable<StrictHttpResponse<OfficeResponse>> {
  const rb = new RequestBuilder(rootUrl, renameRoom.PATH, 'patch');
  if (params) {
    rb.path('officeId', params.officeId, {});
    rb.path('roomId', params.roomId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfficeResponse>;
    })
  );
}

renameRoom.PATH = '/offices/{officeId}/rooms/{roomId}/name';
