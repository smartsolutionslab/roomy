/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AddRoomRequest } from '../../models/add-room-request';
import { RoomResponse } from '../../models/room-response';

export interface AddRoom$Params {
  officeId: string;
      body: AddRoomRequest
}

export function addRoom(http: HttpClient, rootUrl: string, params: AddRoom$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomResponse>> {
  const rb = new RequestBuilder(rootUrl, addRoom.PATH, 'post');
  if (params) {
    rb.path('officeId', params.officeId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomResponse>;
    })
  );
}

addRoom.PATH = '/offices/{officeId}/rooms';
