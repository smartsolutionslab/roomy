/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfficeResponse } from '../../models/office-response';
import { RelocateOfficeRequest } from '../../models/relocate-office-request';

export interface ChangeOfficeLocation$Params {
  officeId: string;
      body: RelocateOfficeRequest
}

export function changeOfficeLocation(http: HttpClient, rootUrl: string, params: ChangeOfficeLocation$Params, context?: HttpContext): Observable<StrictHttpResponse<OfficeResponse>> {
  const rb = new RequestBuilder(rootUrl, changeOfficeLocation.PATH, 'patch');
  if (params) {
    rb.path('officeId', params.officeId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfficeResponse>;
    })
  );
}

changeOfficeLocation.PATH = '/offices/{officeId}/location';
