{
  "name": "organization-data-access",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/organization/data-access/src",
  "prefix": "roomy",
  "projectType": "library",
  "tags": ["type:data-access", "context:organization"],
  "targets": {
    "generate-client": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ng-openapi-gen --config libs/organization/data-access/ng-openapi-gen.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/organization/data-access"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
