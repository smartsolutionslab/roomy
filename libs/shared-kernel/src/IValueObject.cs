namespace SmartSolutionsLab.Roomy.SharedKernel;

// Marker for value objects: immutable, equal by value, no identity. Tags them so tooling
// (architecture tests, EF Core conventions) can discover them and documents the intent.
public interface IValueObject;
