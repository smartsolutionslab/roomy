# shared-ui

Cross-context presentational components (`type:ui`/`context:shared`, ADR-0035). Hosts the
infinite-scroll list primitive used by every endless list view (ADR-0042/ADR-0024).

## Running unit tests

Run `nx test shared-ui` to execute the unit tests.
