/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OccupancyDayResponse } from '../../models/occupancy-day-response';

export interface ViewOccupancy$Params {
  officeId?: string;
  roomId?: string;
  from?: string;
  to?: string;
}

export function viewOccupancy(http: HttpClient, rootUrl: string, params?: ViewOccupancy$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OccupancyDayResponse>>> {
  const rb = new RequestBuilder(rootUrl, viewOccupancy.PATH, 'get');
  if (params) {
    rb.query('officeId', params.officeId, {});
    rb.query('roomId', params.roomId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OccupancyDayResponse>>;
    })
  );
}

viewOccupancy.PATH = '/occupancy';
